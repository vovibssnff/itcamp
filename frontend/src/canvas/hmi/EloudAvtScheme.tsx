/**
 * Quarantined — SVG mnemonic is not product UX.
 * Use HmiCanvas / ConstructorCanvas with shared EquipmentWidget instead.
 * Visual reference preserved at `_quarantine_EloudAvtScheme.tsx.bak`.
 */
export function EloudAvtScheme(_props: unknown): never {
  throw new Error(
    'EloudAvtScheme is retired. Render template graph via HmiCanvas / ConstructorCanvas.',
  )
}
