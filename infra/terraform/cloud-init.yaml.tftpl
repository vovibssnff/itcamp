#cloud-config
package_update: true
packages:
  - python3
  - python3-apt
  - curl
runcmd:
  - swapoff -a
  - sed -i '/ swap / s/^/#/' /etc/fstab
  - hostnamectl set-hostname "${role}-$(hostname -s | cut -c1-20)"
final_message: "KTC static node role=${role} ready for Ansible / Deckhouse CAPS"
