# Физическое ядро (physics/, faults/, engine/) зависит только от stdlib + numpy —
# это единственная внешняя зависимость, нужная детерминированным тестам ядра.
numpy==2.1.3

# Транспортный слой и наблюдаемость.
fastapi==0.115.6
uvicorn[standard]==0.34.0
grpcio==1.68.1
grpcio-tools==1.68.1
protobuf==5.29.1
prometheus-client==0.21.1
