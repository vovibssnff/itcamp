apiVersion: deckhouse.io/v1alpha1
kind: ModuleConfig
metadata:
  name: istio
spec:
  version: 2
  enabled: true
  settings:
    # mTLS STRICT for application namespaces (enable after apps have sidecars)
    globalVersion: "1.21"
    # dataPlane:
    #   trafficRedirectionSetupMode: CNI
