apiVersion: deckhouse.io/v1alpha1
kind: ModuleConfig
metadata:
  name: cert-manager
spec:
  version: 1
  enabled: true
  # Built-in ClusterIssuers: letsencrypt, letsencrypt-staging, selfsigned
  # Email for LE is configured via Certificate / or ACME account in issuer.
