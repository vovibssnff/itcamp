# Ядро сервиса (анализ действий, RAG, оценка) зависимостей не требует —
# работает на стандартной библиотеке Python 3.10+.
# Ниже — только то, что нужно транспортному слою и наблюдаемости.

fastapi==0.115.6
uvicorn[standard]==0.34.0
grpcio==1.68.1
grpcio-tools==1.68.1
protobuf==5.29.1
prometheus-client==0.21.1

# Индексация PDF-регламента (опционально: можно заранее сложить .txt).
pdfplumber==0.11.4

# Подключение к Picodata по PG-совместимому протоколу.
# Reflection в SQLAlchemy неприменим — работаем raw SQL (см. §7.4 SRD).
psycopg[binary]==3.2.3
